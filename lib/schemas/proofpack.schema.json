{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "required": ["schemaVersion", "signumVersion", "createdAt", "runId", "decision", "summary", "contract", "diff", "checks"],
  "definitions": {
    "envelope": {
      "type": "object",
      "required": ["sha256", "sizeBytes", "status"],
      "properties": {
        "content": {},
        "sha256": { "type": ["string", "null"] },
        "sizeBytes": { "type": "integer", "minimum": 0 },
        "status": { "type": "string", "enum": ["present", "omitted", "error"] },
        "omitReason": { "type": "string" }
      }
    },
    "contractEnvelope": {
      "allOf": [
        { "$ref": "#/definitions/envelope" },
        {
          "type": "object",
          "properties": {
            "fullSha256": { "type": "string" }
          }
        }
      ]
    }
  },
  "properties": {
    "schemaVersion": { "type": "string", "enum": ["4.0", "4.1", "4.2", "4.3", "4.4", "4.5", "4.6", "4.7", "4.8"] },
    "signumVersion": { "type": "string" },
    "createdAt": { "type": "string", "format": "date-time" },
    "runId": { "type": "string", "pattern": "^signum-" },
    "contractId": { "type": "string", "pattern": "^sig-.+", "description": "Links proofpack to its source contract" },
    "decision": { "type": "string", "enum": ["AUTO_OK", "AUTO_BLOCK", "HUMAN_REVIEW"] },
    "summary": { "type": "string" },
    "confidence": {
      "type": "object",
      "properties": {
        "overall": { "type": "number", "minimum": 0, "maximum": 100 }
      }
    },
    "auditChain": {
      "type": "object",
      "properties": {
        "contractSha256": { "type": "string" },
        "approvedAt": { "type": "string" },
        "baseCommit": { "type": "string" }
      }
    },
    "contractSource": {
      "type": "string",
      "enum": ["interactive", "file", "template"],
      "description": "How the contract was provided: interactive (generated in session), file (pre-approved via SIGNUM_CONTRACT_PATH), template (from a reusable template)"
    },
    "ciContext": {
      "type": "object",
      "properties": {
        "provider": { "type": "string", "enum": ["github-actions", "gitlab-ci", "circleci", "local"] },
        "runUrl": { "type": "string", "format": "uri" },
        "prNumber": { "type": "integer" },
        "triggerEvent": { "type": "string" }
      },
      "additionalProperties": false
    },
    "baselineComparison": {
      "type": "object",
      "properties": {
        "previousRunId": { "type": "string" },
        "previousDecision": { "type": "string", "enum": ["AUTO_OK", "AUTO_BLOCK", "HUMAN_REVIEW"] },
        "previousConfidence": { "type": "number", "minimum": 0, "maximum": 100 },
        "confidenceDelta": { "type": "number" },
        "newFindings": { "type": "integer", "minimum": 0 },
        "resolvedFindings": { "type": "integer", "minimum": 0 }
      },
      "additionalProperties": false
    },
    "contract": { "$ref": "#/definitions/contractEnvelope" },
    "diff": { "$ref": "#/definitions/envelope" },
    "baseline": { "$ref": "#/definitions/envelope" },
    "executeLog": { "$ref": "#/definitions/envelope" },
    "approval": { "$ref": "#/definitions/envelope" },
    "checks": {
      "type": "object",
      "required": ["mechanic", "reviews", "auditSummary"],
      "properties": {
        "mechanic": { "$ref": "#/definitions/envelope" },
        "holdout": { "$ref": "#/definitions/envelope" },
        "policy_scan": { "$ref": "#/definitions/envelope" },
        "reviews": {
          "type": "object",
          "additionalProperties": { "$ref": "#/definitions/envelope" }
        },
        "auditSummary": { "$ref": "#/definitions/envelope" }
      }
    },
    "removalEvidence": {
      "type": "object",
      "description": "Evidence of file/directory removals and cleanup obligation fulfillment (v4.7)",
      "properties": {
        "removals": {
          "type": "array",
          "items": {
            "type": "object",
            "required": ["id", "path", "removed"],
            "properties": {
              "id": { "type": "string", "pattern": "^RM[0-9]+$" },
              "path": { "type": "string" },
              "type": { "type": "string", "enum": ["file", "directory"] },
              "removed": { "type": "boolean" },
              "orphanReferences": { "type": "integer", "minimum": 0 },
              "modulesYamlUpdated": { "type": "boolean" }
            }
          }
        },
        "obligations": {
          "type": "array",
          "items": {
            "type": "object",
            "required": ["id", "fulfilled"],
            "properties": {
              "id": { "type": "string", "pattern": "^CO[0-9]+$" },
              "action": { "type": "string" },
              "fulfilled": { "type": "boolean" },
              "blocking": { "type": "boolean" },
              "verifyOutput": { "type": "string" }
            }
          }
        }
      },
      "additionalProperties": false
    },
    "iterativeAudit": {
      "type": "object",
      "description": "Iterative AUDIT metadata (v4.6+). Present only when AUDIT ran >1 iteration.",
      "properties": {
        "iterationsUsed": { "type": "integer", "minimum": 1 },
        "iterationsMax": { "type": "integer", "minimum": 1 },
        "bestIteration": { "type": "integer", "minimum": 1 },
        "earlyStop": { "type": "boolean" },
        "earlyStopReason": { "type": "string" },
        "terminalReason": { "type": "string" },
        "remainingSeverity": { "type": "string", "enum": ["MINOR", "MAJOR", "CRITICAL", "none"] },
        "auditIterations": {
          "type": "array",
          "items": {
            "type": "object",
            "required": ["pass", "score", "findingsCount", "decision"],
            "properties": {
              "pass": { "type": "integer", "minimum": 1 },
              "score": { "type": "integer", "maximum": 0 },
              "findingsCount": {
                "type": "object",
                "properties": {
                  "critical": { "type": "integer", "minimum": 0 },
                  "major": { "type": "integer", "minimum": 0 },
                  "minor": { "type": "integer", "minimum": 0 }
                }
              },
              "mechanicRegressions": { "type": "boolean" },
              "holdoutFailures": { "type": "integer", "minimum": 0 },
              "decision": { "type": "string" }
            }
          }
        },
        "resolvedFindings": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "fingerprint": { "type": "string" },
              "category": { "type": "string" },
              "file": { "type": "string" },
              "severity": { "type": "string" },
              "resolvedInPass": { "type": "integer" }
            }
          }
        },
        "remainingFindings": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "fingerprint": { "type": "string" },
              "category": { "type": "string" },
              "file": { "type": "string" },
              "severity": { "type": "string" },
              "comment": { "type": "string" }
            }
          }
        }
      },
      "additionalProperties": false
    }
  }
}
